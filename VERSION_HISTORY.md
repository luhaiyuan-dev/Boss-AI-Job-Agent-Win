# Windows Web 版本记录

本文件只记录当前 Windows Web 项目自身的迭代，不继承 Android 项目的版本号。

- `0.0.0`（2026-07-27）：Windows Web 项目初始基线。
- `0.0.1`（2026-07-31）：完成 Windows 端多求职意向、右侧详情读取、消息角标巡检及 Edge CDP 流程修订。
- `0.0.2`（2026-07-31）：修复随机等待审计、未读会话进入与分类、联系方式卡片误点、岗位身份记录、列表卡片误跳完整详情页。
- `0.0.3`（2026-07-31）：修复返回推荐列表后继续使用旧卡片快照的问题，并为 API 响应截断、远端断连补充重试与脱敏失败审计。
- `0.0.4`（2026-07-31）：新增GUI手填字段TXT默认值，启动时自动读取并填充；下拉框默认值保持由程序管理。
- `0.0.5`（2026-07-31）：固定TXT仅在GUI实例初次启动时读取；开始运行及暂停恢复只采用GUI当前输入值。
- `0.0.6`（2026-07-31）：修复未读会话切换尚未完成就读取消息，以及新版附件简历卡片类名未纳入结构化上下文导致的漏发。
- `0.0.7`（2026-07-31）：修复招呼语发送确认被“已读/送达”前缀误判、模型审核期间推荐列表重绘导致岗位失联、虚拟会话节点不可见导致打开与置顶失败；新增页面与元素有界等待，并让简历已发送/已查看及明确拒绝通知直接返回。
- `0.0.8`（2026-07-31）：新增当日已沟通120位BOSS提醒的精确检测与自动关闭；关闭后重新执行被弹窗拦截的“立即沟通”，继续当前岗位进度。
- `0.0.9`（2026-07-31）：GUI运行条件第二行新增网页求职意向下拉框；支持锁定单条意向投递，并以“平均次数”保留原多意向平均分配策略。
- `0.0.10`（2026-08-03）：修复消息巡检返回后丢失GUI指定求职意向、拒绝模板仍被置顶，以及会话操作按钮需悬停却被直接等待导致的未读处理失败。
- `0.0.11`（2026-08-03）：GUI运行条件新增薪资范围和公司规模；薪资按岗位完整区间、本地公司规模档位做硬筛；运行模式默认实际发送、审核方式默认大模型API，并移除页面黑色主标题中的版本号。
- `0.1.0`（2026-08-04）：修复未读会话拒绝/状态通知误置顶、HR主动会话未按真实岗位名/地点/薪资审核、不打招呼公司仍被回复、详情沟通按钮被隐藏旧节点遮蔽、手机端已置顶会话重复置顶报错，以及GUI查看历史结果时被新记录强制拉到底部的问题。
- `0.1.1`（2026-08-04）：新增当日已沟通150位BOSS硬上限的精确检测；自动点击“确定”并正常结束运行，GUI显示结束理由且运行记录、断点同步保存该原因。
- `0.1.2`（2026-08-04）：加固150位沟通上限终止链路，关闭弹窗失败也不会恢复投递；跳过隐藏旧弹窗节点，修复异常运行写回MySQL时统计可能归零，并补齐项目README、验证报告、脱敏配置模板与开发测试依赖。
- `0.1.3`（2026-08-04）：新增 Win-Web 专用离线部署包与一键安装向导，收录本机对应版本的 Python、Edge、MySQL、Navicat、Codex 和完整 wheelhouse；API Key 与 Codex 登录均可跳过，并补充脱敏分发、哈希校验、环境验证和首次使用文档。
- `0.1.4`（2026-08-08）：新增两个可双击运行的 windowed EXE；修复 GUI 周期探测及登录器调用外部进程时出现空终端闪窗的问题；重做并应用清新浅蓝配色的求职助手/登录浏览器图标，图标源图纳入版本控制并在打包时自动重建多尺寸 ICO。
- `0.1.5`（2026-08-08）：修复同路径重建 EXE 后已打开的文件夹仍显示旧图标的问题；打包完成后通过 Shell 定向更新通知失效两个 EXE 的内存图标，并触发系统图标显示刷新，无需清空全部 Explorer 缓存。
- `0.1.6`（2026-08-08）：将正式产物升级为两个 Nuitka onefile EXE，并在仅用于构建的临时源码副本中混淆核心策略字符串；重构离线部署为先安装或复用 PowerShell 7，再安装或复用 Edge、VC++、MySQL、Navicat，另提供可选 Codex 安装与登录检测，在 `requests-packages` 父目录创建不含密钥和数据库凭据的外置配置模板；采用并归档正式 A 方案图标，并在干净 Windows Sandbox 完成一键部署、Navicat 使用及 MySQL 连接验收。

版本号每次更新只加最后一位；末位满 11 后向前一位进位，并将末位归零，即
`0.0.0 → 0.0.1 → … → 0.0.11 → 0.1.0 → … → 0.11.11 → 1.0.0`，不得跳号。
